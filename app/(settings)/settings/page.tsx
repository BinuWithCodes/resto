// Placeholder settings section (super_admin only — gating added with auth in step 0.7 / 0.9).
export default function SettingsPage() {
  return (
    <main className="p-6">
      <h1 className="text-xl font-semibold">Settings</h1>
      <p className="text-muted-foreground mt-2 text-sm">
        Placeholder — super_admin only, gated once auth lands.
      </p>
    </main>
  );
}
